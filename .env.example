# Copy this file to .env and fill in your key.
# Google Maps JavaScript API key (Maps JS API enabled, billing on).
# Vite exposes VITE_-prefixed vars to the client at build time.
VITE_GOOGLE_MAPS_API_KEY=
